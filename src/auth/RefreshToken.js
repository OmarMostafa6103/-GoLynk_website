// ملف لتجديد التوكن
export const refreshToken = async () => {
  // هنا يتم استدعاء API لتجديد التوكن
  // مثال:
  // const response = await axios.post('/api/refresh-token', { token });
  // return response.data;
};
